// Importing the createContext function from the "react" library.
import { createContext } from "react";

// Creating a new context using the createContext function from React.
const cartContext = createContext();

// Exporting the created context to make it available for other parts of the application.
export default cartContext;